// The values for these variables are stored in the .env file
// Please copy `.env.example` to `.env` and replace placeholder values with your actual environment variables.

const apiKey = process.env.GOOGLE_CLOUD_API_KEY;
const cx = process.env.CUSTOM_SEARCH_ENGINE_ID;